# 使用ssh失败，但是使用http成功

ssh端口默认是22，但是公司为了防护，会修改成自定义值，如果没有在本地的 ~/.ssh/config中配置，就会失败，哪怕服务器端就是默认的 22 端口，但**你当前所在的公司/学校内网防火墙**非常严格，在出口处就把 22 端口给彻底封死了（不让你连外网的任何 SSH 服务器），这时候也会失败。

使用ssh需要先在平台配置SSH Keys，但是使用http不需要