# Tokenizer 与文本表示

> **本质**：Tokenizer 把字符串切成模型能计算的最小单元（token），Embedding 把 token 变成向量。它决定了**词表大小、上下文长度成本、多语言效率和数字处理能力**——是工程上天天踩坑的第一道门。

## 核心考点

### 1. 切分粒度的演进

- **词级**：词表爆炸 + OOV（未登录词）问题，废。
- **字符级**：无 OOV，但序列太长，语义太稀。
- **子词（Subword）**：折中，高频词整体保留，低频词再切。**现代 LLM 全是子词**。

### 2. 三大子词算法（能讲清 BPE 就够）

- **BPE（Byte-Pair Encoding，GPT 系）**：从字符开始，迭代**合并语料中最高频的相邻对**，直到词表满。简单、快，事实标准。
  - GPT 系用 **byte-level BPE**：先转字节再合并，**天然无 OOV**（任何 UTF-8 都能表示）。
- **WordPiece（BERT）**：类似 BPE，但按「提升语言模型似然」选合并对，子词加 `##` 前缀。
- **SentencePiece / Unigram（LLaMA、T5）**：把文本当原始流处理（空格变 `▁`），从「大词表反向剪枝」的概率角度切分。对中文/多语言更友好。

### 3. 词表大小的权衡（工程题）

- 词表大 → 序列短（省算力）但 Embedding/LM Head 参数大，低频词学不充分。
- 词表小 → 反之。
- 主流：32K ~ 150K+。**中文模型（Qwen）词表更大以提升中文 token 效率**——同一篇中文，不同模型切出的 token 数差 30-50%，直接影响 API 成本。

### 4. 特殊 token 与 Chat Template

- 基础角色：BOS（开头）/ EOS（结束，**生成停止的信号**）/ PAD（对齐填充）。
- **Chat template**：多轮对话靠角色标记拼成一条序列（`<|im_start|>user ... <|im_end|>`）。**模板用错 = 模型行为诡异**，工程上直接决定多轮对话正确性 → 详见 [07-解码策略与推理基础](07-解码策略与推理基础.md)。

### 5. 为什么 LLM 数不好数、算不好算术（高频趣题）

- 数字被切成「不是按位对齐」的 token（`12345` 可能切成 `123`+`45`），没有位值概念。
- 缓解：模型规模、R1/o1 类推理模型、工具调用（交给代码执行器）。

### 6. 文本表示演进：从 One-Hot 到动态向量

- **One-Hot**：词表维度稀疏向量，词与词正交，无语义。
- **Word2Vec（2013）**：CBOW（上下文猜词）/ Skip-gram（词猜上下文），得到**静态稠密词向量**，「king - man + woman ≈ queen」。缺陷：一词一向量，多义词无法区分（苹果=公司=水果）。
- **上下文相关表示**：ELMo（双向 LSTM）→ **BERT/GPT（Transformer）**：同一个词在不同上下文里向量不同。这是理解「Embedding 模型」的前提 → [09-Embedding与向量检索](09-Embedding与向量检索.md)。

## 面试高频速答

### BPE 的流程？

<details>
<summary>答案</summary>
初始化词表为所有单字符 → 统计语料中相邻符号对的频率 → 合并最高频对进词表 → 重复直到达到目标词表大小。编码时按学到的合并规则从左到右贪心切分。byte-level 版本先转 UTF-8 字节，保证任意文本可编码。
</details>

### Token 数为什么要关心？

<details>
<summary>答案</summary>
三重成本：上下文长度按 token 计（切得碎 → 同样文本占更多窗口）、API 按 token 计费、推理延迟与 token 数成正比。中文在 GPT 系 tokenizer 上 1 汉字常 ≥1 token，用 Qwen/GLM 等中文优化词表更省。
</details>

## 互链

- Chat template 与生成循环：[07-解码策略与推理基础](07-解码策略与推理基础.md)
- 静态词向量 → 现代 Embedding 模型：[09-Embedding与向量检索](09-Embedding与向量检索.md)
