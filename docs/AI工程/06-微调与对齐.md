# 微调与对齐

> **本质**：预训练给模型**知识**，SFT 教它**格式与行为**，RLHF/DPO 教它**偏好与价值观**。工程上 90% 的微调需求用 LoRA 家族解决，选型口诀：**知识问题用 RAG，行为问题才微调**。

## 核心考点

### 1. 训练范式全景（一条主线）

```
Pretrain（下一词预测，学知识）
  → SFT（指令-回答对，学会「听指令干活」）
  → 偏好对齐（RLHF / DPO，学会「回答得让人满意」）
  → 部署
```

> **工程映射**：HuggingFace 生态 = `trl.SFTTrainer`（SFT）→ `trl.DPOTrainer` / `trl.GRPOTrainer`（对齐）；一键化替代 = LLaMA-Factory（只写 yaml）。下面每节给出对应代码。

### 2. SFT 指令微调

- 数据格式：`{instruction, input, output}` 或多轮 messages；训练时套 **chat template**，通常只对 output 部分算 loss（mask 掉 prompt）。
- 数据量：几千~几十万条高质量样本即可显著改变行为；**质量远比数量重要**（LIMA 论文结论）。
- 常见坑：数据格式与模板不一致、epochs 过多（1-3 轮足够）导致过拟合。

**代码落地（TRL / HuggingFace）**

数据文件 `sft_data.jsonl`（messages 对话格式）：

```json
{"messages": [
  {"role": "system", "content": "你是公司客服，只依据知识库回答"},
  {"role": "user", "content": "发票怎么开？"},
  {"role": "assistant", "content": "在「个人中心-订单」页选择订单后点击「申请开票」…"}
]}
```

训练脚本：

```python
from datasets import load_dataset
from trl import SFTTrainer, SFTConfig

ds = load_dataset("json", data_files="sft_data.jsonl", split="train")
# 对话格式的数据集 TRL 会自动套 chat template

trainer = SFTTrainer(
    model="Qwen/Qwen2.5-7B-Instruct",   # 也可传 §3 里量化/加好 LoRA 的 model 对象
    train_dataset=ds,
    args=SFTConfig(
        output_dir="saves/sft",
        num_train_epochs=2,              # 1~3 轮，多了必过拟合
        learning_rate=1e-4,              # LoRA 用大 lr；全参微调改 2e-5
        per_device_train_batch_size=2,
        gradient_accumulation_steps=8,   # 等效 batch=16
        max_length=2048,                 # 旧版 TRL 参数名是 max_seq_length
    ),
    # 只对 assistant 部分算 loss：
    #   新版 TRL：SFTConfig(assistant_only_loss=True)（要求模板含 generation 标记）
    #   旧版 TRL：data_collator=DataCollatorForCompletionOnlyLM("<|im_start|>assistant\n", tokenizer=...)
)
trainer.train()
```

### 3. PEFT：参数高效微调（核心考点 LoRA）

- 全参微调 7B 要 ~60GB+ 显存（权重梯度+优化器状态），成本高。
- **LoRA 原理**：微调时权重变化量是**低秩**的 → 用两个小矩阵乘积近似：
  `W' = W₀ + ΔW = W₀ + B·A`，其中 `W₀` 冻结，`A∈(r×d)` 降维、`B∈(d×r)` 升维，r 常取 8~64。
- 只训 A/B，可训参数 **<1%**；推理时可把 BA 合并回 W₀，**零额外延迟**。
- **QLoRA**：底座模型量化到 4bit（NF4）+ LoRA，单张 24G 卡微调 33B 级模型，个人/小团队标配。
- 其他 PEFT（了解）：Adapter（插入小模块，有推理延迟）、Prefix/Prompt-tuning（学软提示，不改动模型）。
- 落地工具：LLaMA-Factory、HuggingFace PEFT/TRL、Unsloth。

**代码落地（PEFT + bitsandbytes）**

```python
from peft import LoraConfig, TaskType

lora_cfg = LoraConfig(
    task_type=TaskType.CAUSAL_LM,
    r=16, lora_alpha=32,          # alpha≈2r 起步，等效缩放系数 alpha/r
    lora_dropout=0.05,
    # Qwen/LLaMA 系：把全部线性层都挂上 LoRA（只挂 q_proj 会欠拟合）
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj",
                    "gate_proj", "up_proj", "down_proj"],
)

# —— QLoRA：底座 4bit 量化加载（单张 24G 卡可跑）——
import torch
from transformers import AutoModelForCausalLM, BitsAndBytesConfig
from peft import get_peft_model

bnb = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",          # NF4：面向正态分布权重的 4bit 类型
    bnb_4bit_compute_dtype=torch.bfloat16,
)
model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen2.5-7B-Instruct",
    quantization_config=bnb, device_map="auto")
model = get_peft_model(model, lora_cfg)
model.print_trainable_parameters()      # trainable params ≈ 0.5%~1%

# 把 model 传给 §2 的 SFTTrainer 即可，其余代码完全不变
```

训完的 adapter 只有几十 MB，上线两种方式：

```python
# ① 合并进底座：推理零额外延迟，产物可被 vLLM 直接加载
model.merge_and_unload().save_pretrained("qwen2.5-7b-merged")
```

```bash
# ② 不合并，一套底座挂 N 个业务 LoRA 热切换（多租户省钱姿势）
vllm serve Qwen/Qwen2.5-7B-Instruct --enable-lora --lora-modules sft=saves/sft
```

**LLaMA-Factory 零代码版**（写 yaml 即训练，上面全部流程等价于）：

```yaml
# train_lora.yaml → llamafactory-cli train train_lora.yaml
model_name_or_path: Qwen/Qwen2.5-7B-Instruct
stage: sft
finetuning_type: lora
lora_rank: 16
lora_alpha: 32
lora_target: all
template: qwen
dataset: my_sft              # 在 dataset_info.json 中登记
num_train_epochs: 2
learning_rate: 1.0e-4
output_dir: saves/qwen-lora
```

### 4. 对齐：RLHF 与 DPO（对比着记）

| | RLHF（PPO 时代标准） | DPO（新主流） |
|---|---|---|
| 流程 | 训奖励模型 RM（人类对回答排序）→ 用 PPO 强化学习优化策略 | **跳过 RM 和 RL**，直接用偏好对 (chosen, rejected) 做监督式损失优化 |
| 优点 | 能持续在线优化，上限高 | 简单稳定、好实现、不易崩 |
| 缺点 | 流程复杂、训练不稳、reward hacking（钻奖励空子） | 离线数据，上限略低 |
- 工程认知：开源模型微调社区现在**默认 DPO/KTO**；RLHF 里的 PPO 细节面试问到能说出上面三点即可。

**代码落地（TRL DPOTrainer）**

数据 `dpo_pairs.jsonl`：`{"prompt": "...", "chosen": "好答案", "rejected": "差答案"}`。

```python
from trl import DPOTrainer, DPOConfig

ds = load_dataset("json", data_files="dpo_pairs.jsonl", split="train")

trainer = DPOTrainer(
    model="Qwen/Qwen2.5-7B-Instruct",
    args=DPOConfig(
        output_dir="saves/dpo",
        beta=0.1,                # 越大越贴近参考模型（越保守），常用 0.05~0.1
        learning_rate=5e-6,      # DPO 学习率远小于 SFT
        num_train_epochs=1,
    ),
    train_dataset=ds,
    peft_config=lora_cfg,        # LoRA 模式下底座即隐式参考模型，不用加载第二份
)
trainer.train()
```

- RLHF(PPO) 在 TRL 里对应 `RewardTrainer`（训 RM）+ `PPOTrainer`（强化），现在很少有人手写；DeepSeek R1 式的 **GRPO** 对应 `GRPOTrainer`，是可考的新名词。

### 5. 灾难性遗忘（微调必踩的坑）

- 现象：领域数据微调后，通用能力明显下降。
- 缓解：混入一定比例通用数据、小学习率、少 epochs、**LoRA 本身就是天然缓解**（原权重不动）。

**代码落地（数据混洗）**

```python
from datasets import interleave_datasets
# 领域数据 30% + 通用数据 70% 交错混洗，是防遗忘最简单有效的手段
train_ds = interleave_datasets([domain_ds, general_ds],
                               probabilities=[0.3, 0.7], seed=42)
```

### 6. 微调 vs RAG 选型（必考决策题）

| 需求 | 方案 |
|------|------|
| 知识新 / 私有知识 / 要溯源 | **RAG**（见 [10-RAG系统](10-RAG系统.md)） |
| 行为、格式、语气、领域风格、稳定输出 JSON | **SFT/LoRA** |
| 输出对齐人类偏好 | DPO |
| 实际生产 | 常组合：RAG 管知识 + 轻量微调管行为 |

- 记忆点：**微调改变「怎么答」，不擅长塞「事实」**——硬塞事实容易幻觉且难更新。
- 落地链路即 §3 的两种部署方式：合并权重走 vLLM 常规服务；多业务则共享底座 + 多 adapter 热切换。

## 面试高频速答

### LoRA 为什么有效？rank 怎么选？

<details>
<summary>答案</summary>
假设：微调引起的权重变化在低维子空间内（任务适应是低秩的），所以用 r 秩的两个矩阵近似 ΔW 足够。rank 选择：简单任务/格式对齐 r=8~16；领域迁移重、数据多 r=32~64+，配合 alpha（缩放）调节。经验：先小 r 跑通，不够再加大。
</details>

### RLHF 和 DPO 的区别一句话？

<details>
<summary>答案</summary>
RLHF 先训一个奖励模型再用强化学习（PPO）去最大化奖励；DPO 从数学上证明可以绕过 RM 与 RL，直接用偏好数据对做类似分类的稳定优化。前者上限高但难训，后者简单稳定，成为开源默认。
</details>

### 什么时候绝不该选微调？

<details>
<summary>答案</summary>
需求本质是「知识」时：知识频繁更新、需要引用来源、错误代价高——这些用 RAG。微调塞知识成本高、难更新、还会加重幻觉。
</details>

## 互链

- 底座从哪来：[05-预训练与模型格局](05-预训练与模型格局.md)
- 行为问题的另一解法：[11-Prompt工程](11-Prompt工程.md)
- 知识问题的正解：[10-RAG系统](10-RAG系统.md)
