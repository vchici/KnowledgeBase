**流式架构与长连接 (Streaming, SSE, WebSocket)**：

- **学习重点**：**SSE (Server-Sent Events)** 的协议原理，以及如何在多 Agent 协作（Multi-Agent System）中实现中间思考过程的“分流”和“聚合”输出。
    
- **核心痛点**：长文本必须像打字机一样一点点吐出来，工业上还要同时吐出 `[正在调用工具A]` $\to$ `[正在分析数据B]` 的系统事件流。