**完全异步化与事件驱动 (Asyncio & Event-Driven)**：

- **学习重点**：Python 的 `asyncio` 高级特性、Celery / RabbitMQ 异步任务队列。
    
- **核心痛点**：大模型生成一个大段回复可能要 15 秒，普通的同步阻塞编程会直接搞垮服务器。如何让 Agent 在后台默默调用 5 个慢速 API，同时前台还能给用户返回丝滑的 Loading 状态。