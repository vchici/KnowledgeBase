**高吞吐量推理后端接入 (LLM Serving API)**：

- **学习重点**：理解 **vLLM**、**TGI** 或 **Ollama** 的 `PagedAttention`、连续批处理 (Continuous Batching) 机制，并学会在业务层做动态限流（Rate Limiter）和多模型分流器。
    
- **核心痛点**：如何避免当 100 个用户同时发起 Agent 请求时，后端大模型实例直接报 429 (Too Many Requests) 或 OOM (内存溢出) 崩溃。