**状态持久化与断点恢复 (State Persistence & Checkpoints)**：

- **学习重点**：学习如何在分布式环境（如 Redis、PostgreSQL）中设计 Agent 的线程锁（Thread Lock）和状态快照机制。
    
- **核心痛点**：当一个复杂任务需要执行数小时，中途网络中断或需要人工审批（Human-in-the-loop）挂起时，系统必须能**完美恢复到断点位置继续运行**。